<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lh.zksockets">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 开机自启动 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />

        <service android:name=".SocketService" />

        <activity android:name=".ui.BaseSetingActivity" />
        <activity android:name=".ui.AdvancedSetingActivity" />
        <activity android:name=".ui.XiangDaoActivity" />
        <activity android:name=".ui.ProjectorSetingActivity" />
        <activity android:name=".ui.ComputerActivity" />
        <activity android:name=".ui.PowerBoxActivity" />
        <activity android:name=".ui.ICcardActivity" />
        <activity android:name=".ui.UsersActivity" />
        <activity android:name=".ui.NetStatusActivity" />
        <activity android:name=".ui.PowerDeviceActivity" />
        <activity android:name=".ui.ZhongKongActivity" />

        <receiver android:name=".BootRestartReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity android:name=".ui.IOsetActivity" />
        <activity android:name=".ui.EventActivity" />
        <activity android:name=".ui.EventSelectActivity"></activity>
    </application>

</manifest>